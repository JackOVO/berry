(function() {
  'use strict';

  angular
    .module('pf.condition')
    .factory('conditionService', conditionService);

  conditionService.$inject = ['$rootScope'];
  function conditionService($rootScope) {
    var service = {};
    return service;

    
  }

})();